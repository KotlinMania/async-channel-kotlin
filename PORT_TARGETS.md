# Port targets — `async-channel-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `async-channel` that codex-rs uses, and therefore the minimum subset that `async-channel-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-15] (rank `113`, tier `direct`, status `To Do`)

**Cargo package name**: `async-channel`
**Rust module name (in `use` statements)**: `async_channel`

## Summary

- **6** distinct symbols imported by codex-rs
- **17** total `use` statements
- **12** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `async_channel::Sender`

- **7 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `codex-mcp`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-mcp/src/connection_manager.rs:38` (`codex-mcp`) — `use async_channel::Sender`
- `codex-mcp/src/elicitation.rs:17` (`codex-mcp`) — `use async_channel::Sender`
- `codex-mcp/src/rmcp_client.rs:39` (`codex-mcp`) — `use async_channel::Sender`
- `core/src/codex_delegate.rs:5` (`codex-core`) — `use async_channel::Sender`
- `core/src/exec.rs:12` (`codex-core`) — `use async_channel::Sender`
- `core/src/realtime_conversation.rs:8` (`codex-core`) — `use async_channel::Sender`
- `core/src/session/mod.rs:45` (`codex-core`) — `use async_channel::Sender`

### `async_channel::Receiver`

- **4 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/codex_delegate.rs:3` (`codex-core`) — `use async_channel::Receiver`
- `core/src/realtime_conversation.rs:6` (`codex-core`) — `use async_channel::Receiver`
- `core/src/session/handlers.rs:5` (`codex-core`) — `use async_channel::Receiver`
- `core/src/session/mod.rs:44` (`codex-core`) — `use async_channel::Receiver`

### `async_channel::unbounded`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `codex-mcp`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-mcp/src/mcp/mod.rs:18` (`codex-mcp`) — `use async_channel::unbounded`
- `core/src/connectors.rs:10` (`codex-core`) — `use async_channel::unbounded`

### `async_channel::bounded`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/codex_delegate_tests.rs:4` (`codex-core`) — `use async_channel::bounded`
- `core/src/realtime_conversation_tests.rs:6` (`codex-core`) — `use async_channel::bounded`

### `async_channel::RecvError`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/realtime_conversation.rs:7` (`codex-core`) — `use async_channel::RecvError`

### `async_channel::TrySendError`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/realtime_conversation.rs:9` (`codex-core`) — `use async_channel::TrySendError`
