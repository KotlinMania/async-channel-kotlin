# Rust callers — `async-channel-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `async-channel` | `async_channel` | 17 | 2 | 1 | 17 |

## `async-channel`

### Inbound symbols (6 distinct, 17 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `async-channel`.

#### `async_channel::Sender` — 7 site(s) (7 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-core` `src/codex_delegate.rs:5` — `use async_channel::Sender`
- ✅ `codex-kotlin/codex-core` `src/exec.rs:12` — `use async_channel::Sender`
- ✅ `codex-kotlin/codex-core` `src/realtime_conversation.rs:8` — `use async_channel::Sender`
- ✅ `codex-kotlin/codex-core` `src/session/mod.rs:45` — `use async_channel::Sender`
- ✅ `codex-kotlin/codex-mcp` `src/connection_manager.rs:38` — `use async_channel::Sender`
- ✅ `codex-kotlin/codex-mcp` `src/elicitation.rs:17` — `use async_channel::Sender`
- ✅ `codex-kotlin/codex-mcp` `src/rmcp_client.rs:39` — `use async_channel::Sender`

#### `async_channel::Receiver` — 4 site(s) (4 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/codex_delegate.rs:3` — `use async_channel::Receiver`
- ✅ `codex-kotlin/codex-core` `src/realtime_conversation.rs:6` — `use async_channel::Receiver`
- ✅ `codex-kotlin/codex-core` `src/session/handlers.rs:5` — `use async_channel::Receiver`
- ✅ `codex-kotlin/codex-core` `src/session/mod.rs:44` — `use async_channel::Receiver`

#### `async_channel::bounded` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/codex_delegate_tests.rs:4` — `use async_channel::bounded`
- ✅ `codex-kotlin/codex-core` `src/realtime_conversation_tests.rs:6` — `use async_channel::bounded`

#### `async_channel::unbounded` — 2 site(s) (2 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-core` `src/connectors.rs:10` — `use async_channel::unbounded`
- ✅ `codex-kotlin/codex-mcp` `src/mcp/mod.rs:18` — `use async_channel::unbounded`

#### `async_channel::RecvError` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/realtime_conversation.rs:7` — `use async_channel::RecvError`

#### `async_channel::TrySendError` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/realtime_conversation.rs:9` — `use async_channel::TrySendError`
